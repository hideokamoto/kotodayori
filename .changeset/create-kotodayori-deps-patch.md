---
"create-kotodayori": patch
---

Update runtime dependencies to latest major versions

`cac` upgraded to v7, `execa` to v9, `ora` to v9. The CLI interface and
generated project templates are unchanged.
