# NOTE: Do NOT enable `provenance=true` here.
# Releases publish from CircleCI via npm Trusted Publishing (OIDC), and CircleCI
# does not support automatic provenance generation. Setting provenance=true makes
# `npm publish` fail with: EUSAGE Automatic provenance generation not supported
# for provider: CircleCI.
provenance=false
